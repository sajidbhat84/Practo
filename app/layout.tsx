import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Practo - Your Home for Health",
  description:
    "Find and book appointments with verified doctors. Get expert medical advice, order medicines, and access your health records.",
  keywords: "doctors, healthcare, medical consultation, book appointment, health records",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
